server:
  port: 8080
spring:
  application:
    name: customer
  datasource:
    password: 'ticaret'
    url: jdbc:postgresql://0.0.0.0:5051/customer
    username: 'ticaret'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true